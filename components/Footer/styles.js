import styled from 'styled-components';

export const Credit = styled.footer`
  text-align: center;
  font-size: 0.8rem;
  padding-bottom: 20px;

  a {
    color: inherit;
    text-decoration: none;
    font-weight: bold;
  }

  a:hover {
    opacity: 0.8;
  }
`;
